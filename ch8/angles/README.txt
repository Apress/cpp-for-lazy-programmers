To run with MinGW, copy Makefile.mingw to Makefile; make; and enter: bash runw

To run with Unix, copy Makefile.unix to Makefile; make; and enter:   ./runx

To run with Microsoft Visual studio, open SSDL_Project.sln and go from there.